import React from "react";

import { Layout } from "@components/common";

import styles from "./index.module.scss";

const Analytics: React.FC = () => {
  return <Layout>This is Analytics Page</Layout>;
};

export default Analytics;
