import React from "react";

import { Layout } from "@components/common";

import styles from "./index.module.scss";

const BlockExplorer: React.FC = () => {
  return <Layout>This is Block Explorer Page</Layout>;
};

export default BlockExplorer;
